#include <iostream>

int main()
{
	int arr[100];
	int n, a, b;
	int size = 0;

	while (a != EOF || b != EOF)
	{
		std::cin >> a >> b;
		arr[size] = a;
		size++;
		arr[size] = b;
		size++;
	}

	for (int i = a; i <= b; i++)
	{
		while (a != 1)
		{
			if (a % 2)

		}
	}

	std::cout << i << j;

	return 0;
}